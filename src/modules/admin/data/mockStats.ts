export interface DashboardStats {
    activeSessions: number;
    respondents: number;
    listeners: number;
}

export const mockStats: DashboardStats = {
    activeSessions: 3,
    respondents: 3,
    listeners: 3,
};
